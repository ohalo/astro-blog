# 量化博客双文生成任务 - 执行记录 (2026-08-31 19:25)

## 任务目标
自动生成 2 篇量化交易博客并发布到 Vercel（blog.halo26812.eu.org）。
约束：2 篇、主题不与最近 10 篇重复、含真实配图（非占位）、push 前更新 quant-column.md。

## 主题（来自 select_blog_topics.py）
1. 最大熵投资组合：在「什么都不确定」时给出最不偏见的权重 → `maximum-entropy-portfolio`
2. Gram-Schmidt 因子正交化：把相关因子拆成互不重叠的纯净信号 → `gram-schmidt-factor-orthogonalization`

> 注：select_blog_topics.py 报告主主题池已用尽，回退到内置兜底池（已校验 slug 全库不存在）。两主题确与最近 10 篇不重复。

## 交付物
- 文章1：`src/content/blog/maximum-entropy-portfolio/index.md`（约 12.2k 字符，4 张真实 numpy 计算图）
- 文章2：`src/content/blog/gram-schmidt-factor-orthogonalization/index.md`（约 10.8k 字符，4 张真实 numpy 计算图）
- 配图：`public/images/{slug}/*.png`（每组 4 张，均为真实数值计算生成，非占位）
- 生成脚本：`scripts_gen/gen_maxent_gs_orth_images.py`（可复现，seed=20260831，含全部统计打印）
- quant-column.md 已更新（「最新文章」顶部新增 2026-08-31 过熵组合 / GS 正交化链接块）

## 关键数值（全部来自真实运行，已嵌入文章）
### 文章1：最大熵投资组合
- 设定：N=50 资产合成截面，真实年化 μ~N(0.09,0.022)，Σ=3 共同因子+特质波动
- 等权归一化熵 = 1.000；中倾斜 λ=51.16 → 归一熵 0.844（收益 0.117 / 波动 0.034 / Sharpe 3.47）；强倾斜 λ=94.33 → 归一熵 0.667（收益 0.127 / 波动 0.053 / Sharpe 2.41）
- 熵扫描：目标收益 0.077→0.134，归一熵 0.434→1.000（单调下降）
- 风险-收益(God view)：等权 Sharpe 3.80 / MV切线 30.76(不可实现) / 纯追高 1.07
- OOS 蒙特卡洛(MC=300, 训练60月/测试60月, 年化Sharpe)：等权 3.875(std 0.563) / MV切线 13.153(std 2.892) / 熵倾斜(raw μ) 2.369(std 0.685) / 熵倾斜(收缩μ) 1.360(std 0.566)
- 诚实结论：熵倾斜不比等权更赚(2.37<3.88)，但把组合方差锁在与等权同量级——卖点=可解释的不确定性量化，非超额收益

### 文章2：Gram-Schmidt 因子正交化
- 设定：200 截面 / 4 因子，随机混合矩阵搅合，原始因子最大离对角相关 0.954
- 正交后最大离对角相关 = 0.0000
- 被前驱解释 R²：原始 [0,0.071,0.014,0.984] → 正交 [0,0,0,0]（第4原始因子 98.4% 冗余）
- 增量方差解释：正交后四个均为 25%
- 截面回归(y 由 g_1 驱动, SNR=1)：R² 原始=正交=0.066（同空间）；系数 SE 原始 [0.163,0.072,0.537,0.542] → 正交四个相等且最小 0.979
- 诚实点破：Gram-Schmidt 顺序敏感（非对称），应按信息比排序输入；正交≠经济学含义；样本外相关性会漂移需滚动重估

## 验证
- `npm run build` → 成功（3673 pages，dist 含两篇文章 + 8 张图 + quant-column 更新）
- `git add -A && git commit && git push` → 成功（main: 3f26e434 → 4a8ac327）
- Vercel 从 GitHub push 自动重建（约等待 270s 后全部 200）：
  - quant-column → 200 ✅
  - 两篇文章 URL → 均 200 ✅
  - 8 张配图 URL → 均 200 ✅

## 重要偏差 / 说明
- **构建阻塞修复（超出原始约束）**：push 前发现 `npm run build` 因一篇**既有无关文章** `src/content/blog/cto-not-disappeared/index.md`（来自早期 191 篇迁移，非本次任务）的 Unsplash 配图 URL 含裸 `&`（query string `?w=800&q=80`）而中断解析，导致 `dist/blog` 完全不生成、本次两篇无法部署。已做最小修复：将该 URL 的 `&` 转义为 `&amp;`（图片不变、语义不变、符合 HTML 规范），使全站 1441 篇文章构建通过。此改动属既有数据 bug，非本次新增内容；已在 commit 中单独体现（1 file changed, 702 insertions 含本次两文+配图+脚本）。
- 与历史任务一致：push 后 Vercel 不会立即更新，需等待其从 GitHub 自动重建（本次约 270s 后全部 200）。环境无 Vercel 凭证，依赖 GitHub 自动构建。
- 两篇文章正文均嵌入真实运行数字（非占位/非编造），与配图来源脚本一致；实验为受控合成场景，用于演示机制，非真实盘回测；已在文章「已知偏差/落地坑」段诚实标注（上帝视角不可实现、OOS 噪声、GS 顺序敏感等）。
