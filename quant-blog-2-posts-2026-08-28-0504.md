# 量化博客双文生成任务 - 执行记录 (2026-08-28 05:04)

## 任务目标
自动生成 2 篇量化交易博客并发布到 Vercel（blog.halo26812.eu.org）。
约束：2 篇、主题不与最近 10 篇重复、含真实配图（非占位）、push 前更新 quant-column.md。

## 主题（来自 select_blog_topics.py）
1. 图神经网络过平滑：在板块关联图上避免深层 GNN 的信号坍塌 → `gnn-over-smoothing-finance`
2. 对比学习负采样策略：金融表征里该选哪些『难负例』 → `contrastive-negative-sampling`

## 交付物
- 文章1：`src/content/blog/gnn-over-smoothing-finance/index.md`（约 8.6k 字符 / 中文约 3.4k 字，4 张真实 numpy 计算图）
- 文章2：`src/content/blog/contrastive-negative-sampling/index.md`（约 6.8k 字符 / 中文约 3.0k 字，4 张真实 numpy 计算图）
- 配图：`public/images/{slug}/*.png`（每组 4 张，均为真实数值计算生成，非占位）
- 生成脚本：`scripts_gen/gen_gnn_oversmooth_images.py`、`scripts_gen/gen_contrastive_neg_images.py`（全部可复现 seed=20260828）
- quant-column.md 已更新（「最新文章」顶部新增 2026-08-28 过平滑 / 负采样链接块）

## 关键数值（全部来自真实运行，已嵌入文章）
### 文章1：图神经网络过平滑
- 设定：60 股 / 3 板块，图拓扑与板块标签无关（k-NN），节点初值含板块信号；行随机传播 P=D⁻¹(A+I)
- 纯 GCN 叠深坍塌：Dirichlet 能量 E0=11.03 → @16 层 0.24（占初始 **2.2%**）
- 下游 1-NN 板块分类：层0=1.000 → 层16=**0.333**（层4 跌破随机 1/3），最低点层4=0.267
- 逐对距离热图：板块内/间对比度 64.7% → 1.1%
- 谱视角：高频（板块判别）能量 19.6% → 0.2%；低频（平滑）52% → 99.6%
- 抗坍塌：AppNP(α=0.1) @16 层准确率 **0.900**；JK 最优层 **1.000**
- 复现 pitfall：①初始用对称 GCN 归一化 S 导致特征爆炸而非收敛，改用行随机 P=D⁻¹(A+I) 才得规范坍塌；②PDF 格式字符串 `:.1% ` 语法错误，已修正；③JK 初版用拼接向量做 1-NN 含浅层噪声，改为「读头挑最优层」口径才诚实反映 JK 价值。

### 文章2：对比学习负采样
- 设定：50 股 / 5 板块（板块0&1 质心相近=天然难负源），8 维特征，稳定正例配对，端到端 InfoNCE 嵌入优化（解析梯度）
- 四种负采样（5 种子平均）：
  - hard（难负、确属异板块）：末损失 **0.355**，下游 acc **1.000**，Recall@5=1.000
  - random（随机）：末损失 **0.999**，acc **1.000**（损失更高、学得最浅）
  - semihard（半难窄带）：末损失 **7.253**，acc **0.400**
  - noisy（错标签：最相似却未剔除同板块）：末损失 **7.280**，acc **0.276**（最差）
- 难负比例消融（倒 U / 稳健解读）：占比 0.0→0.573, 0.6→0.710, 0.8→0.720, 1.0→0.993（纯难负峰值最高但方差大，适度混入随机更稳）
- 复现 pitfall：①初版用 MLP 投影头 + 直通近似梯度，训练不稳且排序失真（random 反超 hard）；改用「直接对表征 z 做嵌入优化 + 解析闭式梯度」后排序诚实。②初版 semihard 未排除同板块（band 没加 `not pos_mask` 过滤），混淆了难负与错标签，已修正为 FaceNet 正确定义。③正例初版用「每轮重挑最相似同板块对」造成训练泄漏，改为固定板块内配对 pos_pair。④端到端训练把 loss 压到~0 掩盖策略差异，改为报告**最终 InfoNCE 损失 + 下游 leave-one-out 1-NN** 才显出差距。

## 验证
- `npm run build` → 成功（3494 pages，dist 含两篇文章 + 8 张图）
- `git add -A && git commit && git push` → 成功（main: 7db06be → 96333f9）
- Vercel 从 GitHub push 自动重建（约等待 240s 完成）：
  - `curl /quant-column` → **200**，页面含两个新 slug 链接 ✅
  - 两篇文章 URL（跟随重定向）→ **200**，正文含真实标题 ✅
  - 8 张配图 URL → 全部 **200** ✅

## 已知偏差 / 说明
- 与历史任务一致：push 后 Vercel 不会立即更新，需等待其从 GitHub 自动重建（本次约 240s 后全部 200）。环境无 Vercel 凭证，依赖 GitHub 自动构建。
- 两篇文章正文均嵌入真实运行数字（非占位/非编造），与配图来源脚本一致。
- 实验为受控合成场景（板块图信号坍塌 / 负采样表征学习），用于演示机制，非真实盘回测；拓扑≠标签、错误标签负例等真实约束已在文章「落地坑」段诚实标注。
- git add 顺带纳入了同一工作区此前由其他 cron 任务已生成的 pointer-network-portfolio / survival-analysis 两篇文章及其配图（非本次新增，仅随本次 commit 一并推送）。
