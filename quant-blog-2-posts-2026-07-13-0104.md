# 量化博客双文自动生成 — 2026-07-13 01:04

## 任务
cron `quant-blog-2-posts-simplified`：生成 2 篇量化交易博客并发布到 Vercel（astro-blog 仓库）。

## 主题（脚本选出，不与最近 10 篇重复）
`select_blog_topics.py` 输出 2 个（主题池用尽，走内置兜底但均不与最近 10 篇重复）：
1. **熵池化(Entropy Pooling)：用最小相对熵把主观情景变成概率分布** — slug `entropy-pooling`
2. **低波动异象：低风险股票为何长期跑赢高风险** — slug `low-volatility-anomaly`

## 产出（均含真实计算配图，非占位符，每篇 4 张 matplotlib 图）
- 图片生成脚本：`generate_entropy_pooling_images.py`、`generate_low_volatility_images.py`（纯 numpy 计算，数字嵌入正文）。
- 文章：`src/content/blog/entropy-pooling/index.md`、`src/content/blog/low-volatility-anomaly/index.md`，均含 Python 代码 + frontmatter（title/description≤160字/publishDate='2026-07-13'/tags/language=Chinese/difficulty=advanced）。

### 文1 熵池化（关键真实数字）
- 4000 情景、3 资产（股/债/金），先验等权 p=1/K；先验月均 股 0.58%/债 0.34%/金 0.53%；先验崩盘(股月跌>6%)概率 5.12%。
- **求解器踩坑修复**：初版 `q∝p·exp(−λf)` 符号写反 + 牛顿全步溢出 → 观点完全解错（点质量/反向 λ）；改为 Meucci 约定 `q∝p·exp(+λf)` + 对偶 G(λ) 回溯线搜索，两观点精确命中。
- 观点1（软倾斜，目标股月均 1.2%）：**精确达成 1.200%**，崩盘概率 5.12%→3.66%，KL=0.012（极小扭曲），组合权重 股 17%→29.1%、债 71.6%→59.5%。
- 观点2（压力，崩盘概率抬到 15%）：**精确达成 15.00%**，股月均 0.58%→−0.28%，KL=0.068；组合权重 股 17%→1.6%、债 71.6%→87.2%（强去风险）。
- 压力阶梯：崩盘目标 3%→40%，股票权重单调降、KL 单调升 → 「观点强度=对先验的偏离代价」。

### 文2 低波动异象（关键真实数字）
- 200 股 × 240 月(20年)，市场月均超额 0.50%、波动 4.0%；α=0.003−0.25·(总波动−均值) 内嵌异常（与全库高阶文一致，如 SVJ 嵌入 VRP）。
- CAPM 检验：理论斜率(市场预期溢价) 6.6%/年，实际拟合斜率 **1.7%/年**（近平 → 异象指纹）。
- 波动五分位（Q1低→Q5高）：Q1 年化15.8%/波动15.4%/Sharpe1.06；Q5 年化9.3%/波动29.6%/Sharpe0.32。Sharpe 单调随波动下降。
- 组合：低波动(Q1) 年化15.8%/Sharpe1.06/回撤−13%；高波动(Q5) 9.3%/0.32/−48%；等权全市场 12.3%/0.84/−30%；低波动与全市场相关 0.99（「更好的β」而非对冲）。

## 流程执行
1. 写 2 个图片生成脚本（含真实统计输出），运行生成 8 张图（均验证尺寸正常、非空白）。
2. 写两篇 `index.md`（中文字数约 2200 / 2000，均含代码与 4 图引用）。 entropy-pooling 文初版 description 超 160 字 schema 上限 → 构建报错，已收紧；low-vol 同理修复。
3. 更新 `src/content/pages/quant-column.md`「2026-07-13 新发布」顶部插入 2 条链接。
4. `bun run build` 通过（1304 页，exit 0）。
5. `git add -A && commit && push` → 成功（13 文件，main）。
6. 等待 Vercel 新构建完成（约 2 分钟后生效），验证 `/quant-column`、`/blog/entropy-pooling/`、`/blog/low-volatility-anomaly/` 均 200，8 张图片 CDN 均 200，正文标题/图片引用/quant-column 新链接均已生效。

## 部署状态
✅ 已发布。三页全部 200，8 图全部 200，内容校验通过。

## 备注 / 偏差
- 两文数据均为自洽合成（与全库高阶文一致），用于演示机制；文末均明确标注合成、落地路径与真实陷阱（熵池化：先验质量/约束可识别/过自信/KL预算/BL对比；低波动：市值混淆/换手/利率regime/做空约束/样本外衰减）。
- 熵池化求解器初版有符号错误导致观点解错，已通过 log 空间对偶 + 回溯线搜索修复，两观点现精确命中目标（误差 <1e-3）。
- 低波动模拟低波动组年化 15.8% 偏高（演示用），但异象结构（Sharpe 单调、CAPM 斜率塌陷、回撤更浅）清晰且诚实，文内已说明真实异象来自杠杆约束+行为偏差而非该合成设定。
